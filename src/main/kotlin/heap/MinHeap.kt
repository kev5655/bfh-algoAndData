package heap


//Aufgabe (Challenge):

//a) Implementieren Sie den Abstrakten Datentyp Vorrangwarteschlange (Priority Queue) auf der Basis eines Min-Heaps
//      (vgl. python-Programm sortieren_suchen_new) mit den Operationen
//  insert(prio, el): fügt ein Elementel mit dem Prioritätswert prio ein.
//  accessMin(): liefert das minimale Element zurück.
//  deleteMin(): entfernt das minimale Element aus dem Heap und richtet danach die Heap-Bedingung wieder ein.
//b)Realisieren Sie zudem die folgenden Operationen:
//  decreaseKey(prio, el): setzt den Prioritätswert des Elementes el auf den übergebenen Prioritätswert prio herunter.
//  delete(el): Löscht das Element el aus dem Heap.
//  Für beide Operationen macht man die Annahme, dass ein Zeiger auf das Element im Heap übergeben wird, hier durch el
//  gegeben. Dieser Zeiger könnte natürlich auch ein eindeutiger Schlüsselwert sein.
//c)Vergleichen Sie die angestrebte Realisierung einer Priority Queue mit heapq von python und nehmen Sie dazu Stellung.
//d) Ersetzen Sie die heapq-Operationen im python-File zone_mapping_28-02-23 durch ihre Implementation des ADTs Priority
// Queue im A*-Verfahren. Sie können damit die noch fehlenden Operationen ergänzen (siehe Kommentar im Programmcode).

fun main(args: Array<String>) {



}